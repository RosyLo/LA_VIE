import { SEND_FRIEND_REQUEST } from '../actionTypes';
import { db } from '../../firebase';

export const sendFriendRequest = () => (dispatch) => {
  //1.add friendrequest to master reducer & firebase,
  //2. add requestfriend to user firebase
};
